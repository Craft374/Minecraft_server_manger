# EMSR v1.2

## 새로운 기능

- 기존에 설치된 Minecraft 서버를 폴더 경로로 불러올 수 있습니다.
- 설정에서 JDK 설치 위치를 직접 지정할 수 있습니다.
- 서버 버전에 필요한 Java가 없으면 지정한 위치에 자동 설치합니다.
- Java 설치가 끝나면 별도 조작 없이 서버 실행을 이어갑니다.
- Windows용 `build_windows.bat`와 macOS용 `build_macos.command`를 추가했습니다.

## UI 및 사용성

- 서버 목록, 상태, 콘솔, 빠른 명령을 한 화면에서 확인하도록 UI를 변경했습니다.
- 실행 상태와 필요한 Java 버전을 대시보드에서 확인할 수 있습니다.
- 서버 종료 시 `save-all`과 `stop` 명령을 보내 안전하게 종료합니다.

## 참고

- 새 서버 제작 기능은 다음 업데이트에서 작업할 예정입니다.
- 첫 JDK 다운로드에는 인터넷 연결이 필요합니다.
- Windows 실행 파일과 macOS 앱은 각 운영체제에서 빌드해야 합니다.
